{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan route:cache && php artisan view:cache && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "touch database/database.sqlite && php artisan migrate:fresh --force && php artisan db:seed --force && php -S 0.0.0.0:$PORT public/soh_fix.php",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 300
  }
}