{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan key:generate --force && touch database/database.sqlite && php artisan migrate:fresh --force --seed || true && php -S 0.0.0.0:$PORT public/index.php",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/simple_health.php",
    "healthcheckTimeout": 300
  }
}